{
  "name": "files_isdoc",
  "version": "0.6.0",
  "description": "Preview Czech ISDOC e-invoices in the Nextcloud Viewer",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "vite --mode production build",
    "dev": "vite --mode development build",
    "watch": "vite --mode development build --watch",
    "appstore": "rm -rf build/appstore && mkdir -p build/appstore/files_isdoc && cp -r appinfo img js l10n lib LICENSES README.md CHANGELOG.md build/appstore/files_isdoc/ && rm -f build/appstore/files_isdoc/js/*.map && tar -czf build/files_isdoc.tar.gz -C build/appstore files_isdoc && echo 'Created build/files_isdoc.tar.gz'",
    "test": "vitest run"
  },
  "dependencies": {
    "@nextcloud/axios": "^2.5.0",
    "@nextcloud/l10n": "^3.1.0",
    "fflate": "^0.8.2",
    "pkijs": "^3.4.0",
    "vue": "^2.7.16",
    "xmldsigjs": "^2.8.7"
  },
  "devDependencies": {
    "@nextcloud/browserslist-config": "^3.1.2",
    "@nextcloud/vite-config": "^1.7.2",
    "jsdom": "^29.1.1",
    "vite": "^7.3.1",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "browserslist": [
    "extends @nextcloud/browserslist-config"
  ]
}
